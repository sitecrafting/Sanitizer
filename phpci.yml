build_settings:
    verbose: false
    prefer_symlink: false
    mysql:
            host: "localhost"
            user: "root"
            pass: "password"

setup:
    mysql:
        - "DROP DATABASE IF EXISTS sanitizer;"
        - "CREATE DATABASE sanitizer;"
        - "USE sanitizer;"
    shell:
        command: "mysql -u root -ppassword < %BUILD_PATH%/sql/sanitizer.sql"
    composer:
        action: "install"

test:
    php_spec: